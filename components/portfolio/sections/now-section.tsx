export function NowSection() {
  return (
    <section id="now" className="section wrap now-section">
      <p className="section-kicker">Now</p>
      <p className="now-copy">
        Currently building <strong>Runa</strong>, a custom AI voice agent for
        small businesses — restaurants and dental clinics — that answers phone
        calls and takes orders through real dialogue, built with LiveKit and
        LangGraph.
      </p>
    </section>
  );
}
